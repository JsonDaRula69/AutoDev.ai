/**
 * Tools module — custom crew tools registration.
 *
 * Registers AutoDev-specific tools (gh label transitions, evidence writer,
 * worktree manager, heartbeat). Implemented in sub-plan 3 (T12).
 */
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function register(_pi: ExtensionAPI): void {
  /* implemented in T12 */
}