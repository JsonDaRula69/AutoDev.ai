/**
 * Tmux module — background session orchestration via tmux.
 *
 * Spawns and supervises crew agent sessions in tmux panes for full
 * observability and direct interaction. Implemented in sub-plan 4.
 */
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function register(_pi: ExtensionAPI): void {
  /* implemented in sub-plan 4 */
}