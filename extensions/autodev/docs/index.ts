/**
 * Docs module — immutable reference corpus query.
 *
 * Exposes `.autodev/reference/` as a searchable tool so agents can ground
 * decisions in the architecture/API specs. Implemented in sub-plan 3 (T11).
 */
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function register(_pi: ExtensionAPI): void {
  /* implemented in T11 */
}