/**
 * Delegation module — Nemo dispatch and work routing.
 *
 * Routes work to the right crew agent based on task type and dispatch rules.
 * Implemented in sub-plan 2 (T9).
 */
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function register(_pi: ExtensionAPI): void {
  /* implemented in T9 */
}