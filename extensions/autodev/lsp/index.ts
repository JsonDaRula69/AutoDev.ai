/**
 * LSP module — language server integration for crew agents.
 *
 * Exposes diagnostics, go-to-definition, and find-references to review and
 * implementation agents. Implemented in sub-plan 4.
 */
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function register(_pi: ExtensionAPI): void {
  /* implemented in sub-plan 4 */
}