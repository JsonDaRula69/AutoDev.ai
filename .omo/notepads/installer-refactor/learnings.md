## 2026-06-23 — Task 1: install.sh bootstrap script (installer-refactor Wave 1)

**Done:** Created `install.sh` at repo root — standalone bash bootstrap (`#!/usr/bin/env bash`, `set -euo pipefail`) that auto-installs Bun if missing (via `curl -fsSL https://bun.sh/install | bash`, with `curl` pre-check), exports `PATH="$HOME/.bun/bin:$PATH"`, runs `bun install -g autodev`, verifies `command -v autodev`, and hands off to `autodev doctor`. Made executable. All acceptance criteria pass: `test -x`, `bash -n`, `shellcheck`, and exact grep counts (`bun install -g autodev`=1, `autodev doctor`=1, `command -v autodev`=1, `bun.sh/install`=1). Evidence captured to `.omo/evidence/task-1-installer-refactor.md`.

**Issues / gotchas:**
- **Grep-count collisions:** The plan's acceptance criteria require exact grep counts (=1 for two patterns). The first draft put the literal strings `autodev doctor` and `bun install -g autodev` in header comments and echo/diagnostic messages, inflating the counts to 2 and 5. Fix: reword every comment and user-facing echo so the required literal string appears *only* on the actual command line that runs it. **Lesson for future todos in this plan:** when a plan pins exact grep counts, treat the required strings as reserved — keep them off comments and echo messages unless the line is the command itself.
- **Bun installer PATH:** Confirmed the plan's note — `curl ... bun.sh/install` updates shell rc files but does NOT modify the current shell's `PATH`. The explicit `export PATH="$HOME/.bun/bin:$PATH"` after the install is mandatory; without it the subsequent `bun install -g autodev` would fail with "command not found" under `set -e`.
- **`set -o pipefail` is bash-only** — the `#!/usr/bin/env bash` shebang is load-bearing; do not "port" this to `#!/bin/sh`.

**Next:** Wave 1 task 2 (package.json cleanup + `scripts/cli.ts`) can run in parallel — it's independent of this script.

## 2026-06-23 — Task 2: package.json lifecycle cleanup + scripts/cli.ts entrypoint (installer-refactor Wave 1)

**Done:** Removed dead `preinstall`/`postinstall` scripts and the `preinstall-guard.ts`/`postinstall.ts` files from `package.json` + disk. Repointed `bin.autodev` from `./scripts/postinstall.ts` → `./scripts/cli.ts`. Created `scripts/cli.ts` as a real CLI entrypoint: loads `~/.pi/agent/.env` first (manual KEY=VALUE parser, dynamic `import()` of `getAgentDir` from `@earendil-works/pi-coding-agent` with `~/.pi/agent` fallback), then dispatches subcommands by calling exported functions directly — `runDoctor` from `../extensions/autodev/installer/doctor.js`, `runConfig` from `../extensions/autodev/installer/config-module.js` (todo 4), `getHeartbeatState`/`stopHeartbeat`/`loadRegistry`/`getActiveProject`/`stopAllLoops` from their respective modules. `docs` and `debate` replicate the simple print-message handlers inline. Ported `resolveAuthPath` + `autoNonInteractive` from `installer/index.ts:138-154` (these get deleted in todo 8). All acceptance criteria pass: `grep -c preinstall package.json`=0, `grep -c postinstall package.json`=0, both scripts deleted, `cli.ts` exists, JSON parses, bin.autodev=`./scripts/cli.ts`, `grep -c registerCommands scripts/cli.ts`=0, `CI=1 bun run scripts/cli.ts doctor` exits 0. Typecheck clean.

**Issues / gotchas:**
- **`registerCommands` string collision:** The plan's acceptance criteria require `grep -c "registerCommands" scripts/cli.ts` = 0. The initial doc comment said "NOT via `registerCommands`" — that mention alone inflated the count to 1. Fix: reworded the comment to "NOT via the orchestrator's command-registry API". **Lesson:** grep-count-pinned patterns are reserved strings — keep them out of comments too, not just code.
- **Type-only import in value position:** First draft wrote `const { runDoctor, type DoctorExecFn } = await import(...)` — invalid TS (can't mix value and type in destructured dynamic import). Fix: hoisted `import type { DoctorExecFn }` to top-level, kept `await import` for the runtime value only.
- **Sync `require()` is forbidden:** `resolveAuthPath` in the original `installer/index.ts` used `require("@earendil-works/pi-coding-agent")`. The plan explicitly forbids `require()` (package.json is `"type": "module"`). Fix: made `resolveAuthPath` async and used `await import()`. The doctor/config handlers are already async so this propagates cleanly.
- **Doctor exits non-zero on failure:** The plan requires `scripts/cli.ts doctor` to exit non-zero when `result.failed > 0`. The handler returns `result.failed > 0 ? 1 : 0` and `main()` propagates via `process.exit(code)`. Note: under `CI=1` the local-install guard fires (not a global install) but doctor still exits 0 because `result.failed` stays 0 — the guard is a `notify`, not a check failure.
- **`runConfig` not yet implemented:** `config-module.js` is created in todo 4. The cli.ts `cmdConfig` wraps the dynamic import in try/catch so a missing module surfaces a clean error rather than a crash. When todo 4 lands, no change needed here.

**Next:** Wave 1 tasks 3+ can proceed. Todo 8 will delete `resolveAuthPath`/`autoNonInteractive` from `installer/index.ts` — the copies in `cli.ts` are the new home.

## 2026-06-23 — Task 3: install-module.ts + config-defaults fetchOverride + state "config" scope (installer-refactor Wave 2)

**Done:** Created `extensions/autodev/installer/install-module.ts` exporting `runInstallFixes(deps: InstallModuleDeps): Promise<InstallFixResult[]>` and the `InstallModuleDeps`/`InstallFixResult` interfaces. Phases: (1) install gh/git via `installMissingTools`; (2) download `.pi/` config files via `validateAndCreateConfig(projectRoot, fetchOverride)`; (3) run MC setup (`bunx @cortexkit/magic-context@latest setup --harness pi`, 120s timeout) ONLY when `.pi/magic-context.jsonc` does not already declare `harness: "pi"` — checked via a small JSONC-stripping reader (`hasMagicContextHarnessPi`); (4) run MC doctor (30s timeout) as a warning check that returns `ok:true` with a warning detail on failure (non-fatal). State: marks step 0 (`STEP_TOOLS`) after tools, marks step 3 (`STEP_CONFIG_AND_MC`) only when BOTH config files AND MC setup succeed — recorded in the `"install"` scope. Modified `config-defaults.ts` to accept `fetchOverride?: typeof fetch` as a second param to `validateAndCreateConfig` and wired it through to all three `downloadFile` call sites (settings.json, magic-context.jsonc, agents/*.md). Modified `state.ts` to add `"config"` to the `StateScope` union and `STATE_FILES` map (`config: ".autodev/config-state.json"`) for use by todo 4's config module. All acceptance criteria pass: `test -f install-module.ts`, `bun run typecheck` clean, grep counts match (`runInstallFixes`=1, `InstallModuleDeps`=1, `InstallFixResult`=1, `validateAndCreateConfig(projectRoot`=1, `fetchOverride`=3 in module / 6 in config-defaults, `markStepCompleted(..."install"`=2, `harness === "pi"`=1, `MC_SETUP_CMD`=2, `MC_DOCTOR_CMD`=2), forbidden strings absent (`prompts`=0, `auth.json`=0, `gh auth login`=0). `steps.ts` NOT deleted. `bun test` — all 33 installer tests pass; 487/488 overall (1 pre-existing failure in `test/doctor.test.ts` "doctor detects missing agent files" that also fails on clean `main` before my changes — unrelated).

**Issues / gotchas:**
- **Grep-count collisions (again):** The doc-comment initially said "This module never prompts, writes credentials, or runs `gh auth login`" — those literals inflated the `prompts` and `gh auth login` grep counts to 1 each. Fix: reworded to "never interacts with the user, writes credentials, or performs GitHub authentication". **Reinforces the task-2 lesson:** grep-count-pinned patterns are reserved strings — keep them out of comments too. Audit comments against all acceptance greps before declaring done.
- **JSONC parsing for the MC pre-check:** `.pi/magic-context.jsonc` is JSONC (comments + trailing commas), so `JSON.parse(readFileSync(...))` would throw on a real file. Wrote a minimal `stripJsonc` that handles `//` line comments, `/* ... */` block comments, string-literal preservation, and trailing-comma stripping. Not a full JSONC parser but sufficient for the magic-context schema.
- **MC doctor as warning, not failure:** The plan says MC doctor is a "warning check". Implemented as: on failure, `notify(..., "warning")` and return `{ ok: true, detail: "Warning: ..." }` so the overall install result does not show a failed phase, but the issue is surfaced in both the notify stream and the result detail. This keeps `runInstallFixes` usable as a gate while not hard-failing on a soft health check.
- **Step 3 marking semantics:** The plan says "config files + MC setup = 3 (mark only after both succeed)". Implemented as: `markStepCompleted(STEP_CONFIG_AND_MC)` only fires when `configOk && mcSetupOk`. If config files fail, MC setup is skipped (returns `ok:false`) and step 3 is never marked — so a re-run will retry both. If config files succeed but MC setup fails, step 3 is also not marked — re-run retries MC only (config files are idempotent / will report "already present").
- **Pre-existing test failure confirmed:** `test/doctor.test.ts` "doctor detects missing agent files" fails on clean `main` (verified via `git stash` + isolated `bun test test/doctor.test.ts`). The test deletes `nemo.md` then expects the agents check to be `ok:false`, but `runDoctor` reports `ok:true`. This is a doctor-logic bug unrelated to the install-module work — filed mentally for a separate fix, not this todo.

**Next:** Wave 2 todo 4 (config-module.ts) can proceed in parallel — it will use the new `"config"` StateScope and the `fetchOverride`-enabled `validateAndCreateConfig`. Todo 8 will eventually delete the old `step0ExternalTools`/`step3MagicContext` from `steps.ts` once `runInstallFixes` is wired into the CLI.
## 2026-06-23 — Task 4: config-module.ts interactive secrets + env/auth transformation (installer-refactor Wave 2)

**Done:** Created `extensions/autodev/installer/config-module.ts` — interactive-only config module exporting `runConfig(deps: ConfigModuleDeps, subcommand?: string): Promise<ConfigResult[]>`. `ConfigModuleDeps = { projectRoot, authPath, prompter, notify, execSyncOverride? }` — no `nonInteractive` field. Sub-commands: `llm` (step 2), `voyage` (step 4), `discord` (step 5), `github` (step -1), undefined (run all in sequence). All `.env` writes go to `~/.pi/agent/.env` via `dirname(authPath)`; auth.json gets only `$VAR` references (never literal keys). `llm` handler imports from `~/.pi/agent/auth.json` + `~/.opencode/auth.json` with `$VAR` transformation. `github` handler: checks existing `GH_TOKEN` in `.env` first (verifies via `gh auth status`), prompts with PAT URL + permissions list, writes `GH_TOKEN` to `.env`; if user presses Enter, temporarily `delete process.env.GH_TOKEN` then runs `gh auth login --web` (terminal OAuth fallback). Modified `env.ts`: added optional `envPath?: string` param to `readEnv`, `setEnvVar`, `setEnvVars` (renamed internal helper `envPath`→`defaultEnvPath` to avoid shadow collision). Modified `auth.ts`: `tryImportAuth` now writes actual key to `.env` via `setEnvVar` and writes `$VAR` reference (not literal) to auth.json; added `providerToEnvVar` export (ported from steps.ts). `state.ts` already had `"config"` in `StateScope` (todo 3 or pre-existing) — no change needed. All acceptance criteria pass: `test -f` ✓, typecheck ✓, `runConfig`=1, `execSyncOverride`≥1, `nonInteractive`=0, forbidden-commands=0, `GH_TOKEN`≥1, `gh auth login`≥1, `personal-access-tokens`≥1, `delete process.env.GH_TOKEN`≥1.

**Issues / gotchas:**
- **Grep-count reserved strings in docstrings:** The `nonInteractive` acceptance criterion requires count=0. First draft had "There is no `nonInteractive` mode" in the module docstring, inflating the count to 1. Fix: reworded to avoid the literal token. **Lesson reinforced:** grep-count-pinned patterns are reserved everywhere — comments, docstrings, echo messages. Keep them out of all non-code text.
- **Parameter/helper name collision:** Adding `envPath?: string` param shadowed the existing `function envPath(projectRoot)` helper in env.ts, causing `envPath ?? envPath(projectRoot)` to be a self-reference bug. Fix: renamed the helper to `defaultEnvPath`. Always check for name collisions when adding a param whose name matches an existing local symbol.
- **`providerToEnvVar` placement:** Plan said "port into config-module.ts" but `tryImportAuth` (in auth.ts) also needs it. Putting it in config-module would create a circular import (config-module imports auth). Fix: placed `providerToEnvVar` in auth.ts (where `tryImportAuth` lives) and config-module imports it from there. Single source of truth, no cycle.
- **No-TTY detection is per-handler:** The no-TTY prompter returns "" for every prompt. For `llm`, the provider prompt returning "" triggers the no-TTY warning. For `voyage`, "" is a valid skip (ONNX fallback), so no special no-TTY check is needed there. For `discord`, empty token after confirm=yes triggers no-TTY warning. For `github`, empty token triggers the `gh auth login --web` fallback (legit flow). Each handler decides independently — no global no-TTY gate.
- **`setEnvVars` signature change is backward-compatible:** The new `envPath?` param is last and optional, so all existing 2-arg callers (steps.ts) continue to work unchanged. Verified: typecheck passes with no call-site updates.

**Next:** Wave 3 tasks (5: doctor refactor, 6: can parallelize). Todo 5 will wire `runConfig` into doctor with targeted sub-commands based on failing health checks.

## 2026-06-23 — Test fix: tryImportAuth test expectations updated for $VAR transformation

**Done:** Updated two failing tests in `extensions/autodev/installer/__tests__/installer.test.ts` to match the new `tryImportAuth` behavior (actual key → `.env`, `$VAR` reference → `auth.json`):
1. `tryImportAuth imports from existing auth file` — now asserts `auth["ollama-cloud"]!.key` equals `"$OLLAMA_CLOUD_API_KEY"` and `env.get("OLLAMA_CLOUD_API_KEY")` equals `"sk-imported-key"`.
2. `step2LlmCredentials: interactive imports from existing auth file when user says yes` — same assertions.

**Lesson:** When `tryImportAuth` transforms keys, any test that calls it and checks `auth.json` directly must also check `.env` for the actual key. The `readEnv(dir, join(dir, ".env"))` call is the correct way to verify the `.env` side of the transformation. All 33 installer tests pass.

## 2026-06-23 — Task 6: full sub-command routing in scripts/cli.ts + .env loading in index.ts + remove install/init from orchestrator/cli.ts (installer-refactor Wave 3)

**Done:** Three-file change per plan todo 6:
1. **`scripts/cli.ts`** — Rewrote `cmdConfig`: prints usage listing `llm`/`voyage`/`discord`/`github` when no sub-subcommand (exit 0); otherwise constructs `ConfigModuleDeps` with `prompter = createPrompter()` from `prompts.js` and calls `runConfig(deps, subSubcommand)` (sub-subcommand passed as 2nd arg, NOT inside deps). `prompter.close()` in `finally`.
2. **`extensions/autodev/index.ts`** — Added `loadAgentEnv()` async helper (dynamic `import("@earendil-works/pi-coding-agent")` → `getAgentDir()`, fallback `~/.pi/agent`; manual KEY=VALUE `.env` parse, no-override-existing). Changed `autodevExtension` from sync → `async function ... Promise<void>`. `await loadAgentEnv()` is the FIRST statement, before `pi.on` and the module loop. Confirmed via pi docs (extensions.md:180) that async factories are supported and awaited before startup.
3. **`extensions/autodev/orchestrator/cli.ts`** — Removed `handleInstall`/`handleInit` import + `case "install"`/`case "init"` branches. Added `case "config"` → `handleConfig(parts.slice(1), ctx)`. New `handleConfig`: no-sub → prints usage via `ctx.ui.notify`; otherwise builds `ConfigModuleDeps` (`projectRoot=ctx.cwd??process.cwd()`, `authPath` via dynamic `getAgentDir()` import, `prompter=createPrompter()`, `notify=(msg,level)=>ctx.ui.notify(msg,level)`) and calls `runConfig(deps, subSubcommand)` with `prompter.close()` in `finally`. Updated failure message (line 109) to `"Run \`autodev config\` in an interactive terminal, or re-run \`autodev doctor\`."`. Updated command-description + default-help strings + file-top doc comment to drop install/init, add config.

All acceptance criteria pass: typecheck clean; `handleInstall|handleInit|case "install"|case "init"`=0; `case "config"`=1; `runConfig`=2; `registerCommands` in scripts/cli.ts=0; `autodev install`=0; `handleDebugFlag`=1. `CI=1 bun run scripts/cli.ts doctor` exits 0 (no hang). `bun run scripts/cli.ts config` prints usage listing all 4 sub-commands, exits 0. `bun run scripts/cli.ts badcommand` prints new help list, exits 1. `runConfig(deps, "llm")` with `MockPrompter` (answers: `["ollama-cloud","n","sk-test-key-123"]`) returns `status:"ok"` — verified via test script. Evidence at `.omo/evidence/task-6-installer-refactor.md`.

**Issues / gotchas:**
- **`runConfig` signature mismatch:** The old `cmdConfig` (from todo 2) put `subcommand` and `args` inside the deps object — but `ConfigModuleDeps` (from todo 4) has no such fields; `runConfig(deps, subcommand?)` takes the sub-command as a SEPARATE second parameter. Fix: pass `subSubcommand` as the 2nd arg to `runConfig`, not inside deps.
- **`createPrompter()` cleanup:** `createPrompter()` from `prompts.ts` returns a `Prompter` backed by a readline interface (TTY path) or a no-op (no-TTY path). The readline-backed variant holds an open interface — must call `close()` to avoid leaking the fd / hanging the process. Wrapped the `runConfig` call in `try/finally { prompter.close(); }` in both `cmdConfig` (scripts/cli.ts) and `handleConfig` (orchestrator/cli.ts).
- **Async extension factory:** The plan said "add `.env` loading at the top of the `register()` function" but `autodevExtension` was synchronous and dynamic `import()` (required for `getAgentDir`) is async. Confirmed via pi `docs/extensions.md:180` that pi awaits a factory returning a `Promise` before startup. Changed `export default function` → `export default async function` returning `Promise<void>`. No downstream breakage — pi's extension loader accepts both sync and async factories.
- **`config` no-sub usage path:** The plan requires `bun run scripts/cli.ts config` (no sub-subcommand) to print usage listing `llm`, `voyage`, `discord`, `github` and exit 0. This is a DISTINCT path from `runConfig(deps)` (no sub-command) which would run ALL handlers in sequence. The standalone binary's `config` with no sub-subcommand must NOT launch the interactive all-handlers flow (it would hang in a non-interactive shell). So `cmdConfig` short-circuits to usage BEFORE constructing `ConfigModuleDeps`/calling `runConfig`. The orchestrator's `handleConfig` mirrors this: no-sub → usage via `ctx.ui.notify`, return early.

**Next:** Wave 3 todo 5 (doctor refactor) can proceed — it will wire `runConfig` into doctor with targeted sub-commands based on failing health checks. Todo 7 (test rewrite) is blocked by 6 + 5. Todo 8 (dead-code sweep) will delete `installer/index.ts` now that `handleInstall`/`handleInit` imports are gone.

## 2026-06-23 — Task 5: doctor.ts refactor — orchestrator with install + config modules (installer-refactor Wave 3)

**Done:** Rewrote `extensions/autodev/installer/doctor.ts`. Removed `isGlobalInstall()`, `isFreshInstall()`, the local-install guard (Gate 1), fresh-install gate (Gate 2), and old `runInstall` auto-fix branch (Gate 3). Added `isFirstRun(deps)` — 3-signal check: (a) `auth.json` has a non-empty credential (resolving `$VAR` against `process.env`), (b) `.autodev/install-state.json` has completed steps, (c) `~/.pi/agent/.env` has `OLLAMA_CLOUD_API_KEY`. Used ONLY for messaging (welcome vs "something needs fixing") — never behavioral branching. When `launchConfigFlow: true` and checks fail: calls `runInstallFixes` (always, no prompts, safe in CI), then calls `runConfig` with targeted sub-commands ONLY when `process.stdin.isTTY === true` (LLM fail → "llm", GitHub auth fail → "github", Env fail → "llm" + "voyage" if VoyageAI missing; Discord never auto-triggered), then re-runs all health checks. Prompter created via `createPrompter()` once before first `runConfig`, closed after all sub-commands; `runConfig` does NOT close it. Updated GitHub auth check: verifies `GH_TOKEN` validity via `gh auth status` (passes if succeeds whether or not GH_TOKEN set; fails if GH_TOKEN set but `gh auth status` fails, or if neither). Updated LLM credentials check: resolves `$VAR` references in `auth.json` against `process.env` (a `"$OLLAMA_CLOUD_API_KEY"` entry counts only if `process.env.OLLAMA_CLOUD_API_KEY` is non-empty). Updated Environment vars check: reads from `join(dirname(authPath), ".env")` (was `readEnv(projectRoot)` → project `.env`). Updated Install state check: aggregates `install` + `config` scopes (distinct steps), threshold `>= 6`, detail `X/6` (was `X/8`). Extended `DoctorDeps` with `prompter?: Prompter` and `fetchOverride?: typeof fetch`. All acceptance greps pass: `npm_config_global|isGlobalInstall|isFreshInstall`=0, `isFirstRun`=2, `readAuth|readState|readEnv`=10, `runInstallFixes|runConfig`=5, `isTTY`=1, `autodev config`=1, no `index.js`/`runInit` imports, no `autodev install`/`autodev init` literals. Typecheck clean. Tests: 487/488 pass (1 pre-existing `doctor detects missing agent files` failure — same as clean `main`, unrelated). Updated `test/doctor.test.ts:125` assertion `"2/8"` → `"2/6"` to match the plan-mandated threshold change.

**Issues / gotchas:**
- **Test assertion pinned to old threshold:** `test/doctor.test.ts:125` asserted `stateCheck.detail` contains `"2/8"`. The plan explicitly changes the install-state threshold from 8 to 6 (new install-module records steps 0 and 3; config-module records 2, 4, 5, -1; complete set = {−1, 0, 2, 3, 4, 5} = 6). After the change the detail reads `"2/6 install steps completed"`. Fix: updated the assertion to `"2/6"`. **Lesson:** when a plan changes a numeric threshold, audit test assertions that pin the old number — they will fail and need updating, and that update is in-scope for the todo that changes the threshold (not a separate test-rewrite todo).
- **`exactOptionalPropertyTypes: true` strikes again:** Constructing `InstallModuleDeps`/`ConfigModuleDeps` with `fetchOverride: deps.fetchOverride` (which is `typeof fetch | undefined`) fails typecheck when the target field is typed as `typeof fetch` (not `typeof fetch | undefined`). Fix: spread conditionally — `...(deps.fetchOverride !== undefined ? { fetchOverride: deps.fetchOverride } : {})`. Same pattern for `execSyncOverride`. **Reinforces the task-3 lesson:** under `exactOptionalPropertyTypes`, optional fields on dependency interfaces need conditional spreading, not direct assignment of `undefined`.
- **Prompter lifecycle ownership:** Doctor creates the prompter via `createPrompter()` only when `deps.prompter` is undefined, and closes it after all sub-commands complete. When a prompter is injected (tests), doctor does NOT close it — the caller owns it. `runConfig` never closes the prompter (confirmed in config-module.ts). This prevents the "closed-interface on second call" bug.
- **Targeted sub-commands, not all-handlers:** `runConfig` is called once per failing check with the specific sub-command, never with `undefined` (which would run all 4 handlers including Discord). Discord is not a health check and is never auto-triggered. The `targetedSubcommands()` helper maps check names to sub-commands and dedupes via a Set.
- **Env check now reads from agent dir:** The "Environment vars" check previously called `readEnv(deps.projectRoot)` (reading `projectRoot/.env`). Now reads from `join(dirname(deps.authPath), ".env")` = `~/.pi/agent/.env` — the canonical secrets location. This aligns with config-module and the `.env` loading in `scripts/cli.ts`.
- **Pre-existing agent-files failure persists:** `doctor detects missing agent files` (test/doctor.test.ts:149) fails on clean `main` and remains after this change. Root cause is in the agents check logic (in `validateAndCreateConfig` / config-defaults), not in the doctor orchestration. Deferred to a separate fix.

**Next:** Wave 3 todo 6 (cli.ts full sub-command routing + orchestrator/cli.ts dispatcher rewrite) can proceed — it depends on todo 4 (config-module) which is done, and benefits from todo 5 (doctor) being complete for integration. Todo 7 (test rewrite) will replace the entire `test/doctor.test.ts` suite.

## 2026-06-23 — Task 8: dead-code sweep — delete installer/index.ts + installer/steps.ts (installer-refactor Wave 4)

**Done:** Deleted `extensions/autodev/installer/index.ts` and `extensions/autodev/installer/steps.ts` per plan todo 8. Removed the 508 lines of steps-dependent tests (lines 269-776 of the old 794-line `__tests__/installer.test.ts`) that imported `../steps.js` — all `step1BunCheck`/`step2LlmCredentials`/`step4VoyageAi`/`step5Discord`/`step7KnowledgeBase`/`runInstallSteps` tests — while keeping the 268 lines of state/env/auth/prompts tests (those modules survive). Updated two stale references in `code-review-flow.md`: section 1.3 header now describes the refactored routing through `config-module.ts`/`install-module.ts` (removed `handleInstall()`/`installer/index.ts`/`installer/steps.ts` symbol references), and the F4 bug-table row now notes the old `installer/steps.ts` was removed. Left `scripts/cli.ts:31` provenance comment ("ported from installer/index.ts:138-154, deleted in todo 8") — it documents the deletion, not a live reference. `bun run typecheck` exits 0. Zero references to any of the 21 removed symbols (`runInstall`, `runInit`, `handleInstall`, `handleInit`, `INSTALL_STEPS`, `INIT_STEPS`, `StepContext`, `StepResult`, `step0ExternalTools`, `step0bGhAuth`, `step1BunCheck`, `step2LlmCredentials`, `step3MagicContext`, `step4VoyageAi`, `step5Discord`, `step6GitHubLabels`, `step7KnowledgeBase`, `step8DocsRebuild`, `step9Doctor`, `runInstallSteps`, `runInitSteps`) remain anywhere in the project.

**Issues / gotchas:**
- **Test file was mixed-scope:** `installer.test.ts` tested both surviving modules (`state.js`, `env.js`, `auth.js`, `prompts.js`) and deleted modules (`steps.js`). Per MUST NOT "do not modify files outside the installer dead-code scope" I initially hesitated, but the task explicitly says "If any references remain (including tests), remove them" — so removing the `../steps.js`-importing test blocks was required. Used `awk 'NR<=268 || NR>=776'` to slice cleanly rather than per-block edits (the steps tests spanned 508 contiguous lines with multiple section headers). Kept the `ensureGitignore` integration test at the tail (uses `env.js`, not `steps.js`).
- **Edit-tool anchor pitfall:** First attempt used `edit` to replace just the section header line, which left the test bodies dangling (the replacement only swapped the header, not the block). Switched to `awk` line-range deletion for the large contiguous removal — more reliable than multi-hundred-line `edit` calls.
- **Stale doc references vs. scope guard:** `code-review-flow.md` is a historical review artifact that referenced `handleInstall()` (a removed symbol) and the deleted file paths. The task's "zero references to removed symbols anywhere" requirement overrides the scope guard for stale references. Made minimal edits to reframe section 1.3 as describing the refactored architecture, and annotated the F4 row. Did not rewrite the historical step table — it's labeled as the old sequence now.
- **Provenance comments are not live references:** `scripts/cli.ts:31` mentions `installer/index.ts:138-154` as the source of ported helpers, explicitly noting "deleted in todo 8". This is accurate provenance, not a dangling import. Left it — removing it would lose useful refactor history, and it references no removed symbol.

**Next:** Installer-refactor plan is complete (all 8 todos done). No further work from this plan.
