# Dependencies
node_modules/

# Build output
dist/

# Secrets — never commit credentials
.pi/auth.json

# macOS
.DS_Store

# Logs
*.log

# Debug
.autodev/debug.log

# Runtime SQLite databases (regenerable binary state)
.autodev/decisions/*.db
.autodev/embeddings/*.db

# Session continuation state (runtime only)
.omo/run-continuation/

# OmO working state (plans, evidence, notepads, boulder — runtime/local only)
.omo/

# OpenCode local state (historian exports, node_modules, config — not source)
.opencode/

# Docs corpus (generated, not source)
docs-corpus/